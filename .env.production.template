# Production Environment Variables Template
# Copy this to .env on your production server and update the values

# ===================================
# Django Core Settings
# ===================================

# Generate a new secret key for production using:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY='REPLACE_WITH_STRONG_SECRET_KEY_GENERATED_ABOVE'

# Set to False in production
DEBUG=False

# Add your production domains (comma-separated, no spaces)
ALLOWED_HOSTS=backend.onsync-test.xyz,www.backend.onsync-test.xyz

# ===================================
# Database Settings (Supabase Production)
# ===================================

# Option 1: Use DATABASE_URL (recommended - single connection string)
# Format: postgresql://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL=postgresql://postgres:YOUR_SUPABASE_PASSWORD_HERE@db.iqsugeurjktibkalmsim.supabase.co:5432/postgres

# Option 2: Use individual database settings (legacy - deprecated)
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=YOUR_SUPABASE_PASSWORD_HERE
# DB_HOST=db.iqsugeurjktibkalmsim.supabase.co
# DB_PORT=5432

# ===================================
# CORS Settings
# ===================================

# Frontend domains that can access the API (comma-separated, no spaces)
CORS_ALLOWED_ORIGINS=https://frontend.onsync-test.xyz,https://www.frontend.onsync-test.xyz

# ===================================
# Logging
# ===================================

# Set log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
DJANGO_LOG_LEVEL=INFO

# ===================================
# Optional Settings
# ===================================

# Email settings (if needed for password reset, notifications, etc.)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Sentry for error tracking (optional but recommended)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ===================================
# IMPORTANT NOTES
# ===================================
# 1. Never commit this file to Git (it's in .gitignore)
# 2. Keep file permissions secure: chmod 600 .env
# 3. Backup this file securely
# 4. Use strong passwords for database
# 5. Generate a unique SECRET_KEY for production
