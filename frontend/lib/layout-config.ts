/**
 * Layout Configuration
 * 
 * To switch between layouts, change the LAYOUT_TYPE value:
 * - 'sidebar': Claude-style collapsible sidebar (current)
 * - 'navbar': Traditional top navigation bar
 */

export const LAYOUT_TYPE: 'sidebar' | 'navbar' = 'sidebar';

// To revert to old design:
// 1. Change LAYOUT_TYPE to 'navbar'
// 2. That's it! The app will automatically use the top navbar
